#ifndef MEDIAN_WINDOW_FILTER_H
#define MEDIAN_WINDOW_FILTER_H

#include <stdlib.h>
#include <string.h>

int filter_window_median(int[], int[], int);
static int compare_ints(const void*, const void*);

#endif